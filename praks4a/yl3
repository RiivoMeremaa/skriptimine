echo "Varundan kataloogi './test' faili 'test.backup.tar.gz' kataloogi './backup'."

mkdir -p ./backup

tar -czf ./backup/test.backup.tar.gz ./test

echo "Varundamine lõpetatud. Fail salvestatud: ./backup/test.backup.tar.gz"
